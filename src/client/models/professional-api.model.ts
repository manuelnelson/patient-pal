import {User, Client, Organization} from '../models';
export interface ProfessionalApi {
    //clients: Array<Client>;
    firstname: string;
    lastname: string;
    title: string;
    organization: string;
}
